# チーム開発ガイド

## 基本の流れ

1. Issueで目的、要件ID、受入試験ID、担当、依存関係、完了条件を記録する。
2. 最新の `main` から作業ブランチを作る。
3. 変更と関連文書をコミットし、PRを作る。途中の相談にはDraft PRを使う。
4. 作者以外の最低1人がレビューする。指摘を解消し、確認結果をPRへ記録する。
5. レビュー後にSquash mergeし、不要になった作業ブランチを削除する。

初回のリポジトリ作成を除き、`main` への直接push・force pushは行わない運用とします。
ブランチ保護が設定できないプランでは、これは技術的強制ではなくチームの合意です。

## 名前の付け方

- ブランチ例：`docs/12-payment-flow`、`feat/34-charge`、`fix/56-refund-race`。
- コミット例：`docs: 決済フローの設計書を追加`。
- 1つのPRは1つの目的に絞り、無関係な変更を混ぜない。
- IssueやPRに存在しない担当者名・要件ID・試験結果を書かない。未決は未決とする。

## レビュー

[担当表](docs/team/ownership.md)を基準にレビュー依頼先を選びます。
台帳・金銭処理・認可・認証の変更は、作者以外のバックエンド担当がレビューします。
API契約の変更は利用するFEと提供するBEが影響を確認します。
承認後に内容を変更した場合は、差分の再確認を依頼します。

## 文書を変更するとき

- [要件原本](docs/requirements/event_payment_requirements_integrated_v0.1.md)は保存し、直接書き換えない。
- 要件の変更は[変更記録](docs/requirements/changes.md)と次版を同じPRに含め、現行版へのリンクを更新する。
- 設計書には関連要件ID、状態、担当、レビュー者、更新日を記入する。
- 設計判断は[ADR](docs/adr/README.md)に残す。図の編集元もMarkdown/Mermaid等で管理する。
- 要件ID → 設計書 → Issue/PR → 受入試験ID/証跡を相互参照できるようにする。
- 資料内の過去の委任・作業指示は資料の記述として扱い、現在の依頼範囲を拡張する根拠にしない。

## マージ前の確認

- 関連要件と変更理由、影響範囲が書かれている。
- 文書リンクが開ける。原本や過去の承認済み記録を意図せず変更していない。
- 実装変更時は必要なテストを実施し、未実施の項目・理由を明記する。
- 秘密情報、実利用者の個人情報、本番データを含めていない。

現在は文書のみで、CI・アプリ用のテストコマンドは未導入です。
ローカルでコミット前に `git diff --cached --check` で余分な空白を確認できます。
自動テスト・依存関係検査・秘密情報検査は実装基盤を作る際に導入します。
